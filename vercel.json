{
  "framework": null,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "git": {
    "ignoredBuildStep": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then echo 'Skipping non-main' && exit 0; fi"
  }
}
